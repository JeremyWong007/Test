/*
语言：
SQL

数据库：
SQLite (关系型数据库)
MySQL (关系型数据库)
MongoDB (非关系型数据库)
Multi-Index EOS多索引表，来源于boost库的boost::multi_index，但又有一些重要不同

存储引擎：
rocksdb

*/
#include "../common.h"

class test_db
{
private:
    /* data */
public:
    test_db(/* args */);
    ~test_db();
};

test_db::test_db(/* args */)
{
}

test_db::~test_db()
{
}
